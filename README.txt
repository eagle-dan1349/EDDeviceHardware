Simple identificator of device model
Was coded when I had a project requiring different behaviour for different device models.
Files you need are EDDeviceHardware.h and EDDeviceHardware.m
They provide a singleton class that gets the device model information.